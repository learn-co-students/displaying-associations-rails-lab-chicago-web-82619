<h1>All songs</h1>

<ul>
  <% @songs.each do |song| %>
    <li><%= link_to song.artist.name, artist_path(song.artist) %> - <%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>

<%= link_to "new artist", new_artist_path %>
<%= link_to "new song", new_song_path %>
